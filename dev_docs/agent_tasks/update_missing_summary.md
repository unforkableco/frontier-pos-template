# AI Agent task - update dev_tree.json

Execute the following steps:

1/ Launch the dev_docs/update_dev_tree.sh
2/ Look for empty "summary" entries in dev_docs/dev_tree.json
3/ For every empty summary, read the file in question and fill the summary entry. If the file is a rust file, also add or update the "uses" entry for that file